{
  "description": "Copie timestamp vers @timestamp pour compatibilité Kibana",
  "processors": [
    {
      "set": {
        "field": "@timestamp",
        "value": "{{timestamp}}"
      }
    }
  ]
}
